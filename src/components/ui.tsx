import Link from "next/link";
import { Icon } from "./icons";
export function PageHead({ eyebrow, title, description, action }: {eyebrow?:string;title:string;description:string;action?:React.ReactNode}) { return <div className="mb-8 flex flex-col gap-5 md:flex-row md:items-end md:justify-between"><div>{eyebrow&&<div className="mb-2 text-xs font-extrabold uppercase tracking-[.16em] text-[#176b5b]">{eyebrow}</div>}<h1 className="serif max-w-3xl text-3xl font-semibold leading-tight md:text-[42px]">{title}</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-[#687871] md:text-base">{description}</p></div>{action}</div> }
export function Notice(){return <div className="mb-6 flex gap-3 rounded-xl border border-[#e7d7a4] bg-[#fff8e5] p-4 text-sm text-[#695a2e]"><Icon name="info"/><p><strong>Conteúdo demonstrativo.</strong> Os dados são fictícios e ainda não foram validados profissionalmente. A ferramenta apoia — e não substitui — a decisão do profissional.</p></div>}
export function NextButton({href,label}: {href:string;label:string}) {return <Link className="btn btn-primary" href={href}>{label}<Icon name="arrow" size={18}/></Link>}
export function Status({kind}:{kind:"potencial"|"em-desenvolvimento"|"consolidado"}) {const map={potencial:["Potencial para explorar","bg-[#f7e6b8] text-[#765b15]"],"em-desenvolvimento":["Em desenvolvimento","bg-[#dcece6] text-[#176b5b]"],consolidado:["Consolidado no contexto","bg-[#e5e8ed] text-[#59636f]"]};return <span className={`pill ${map[kind][1]}`}>{map[kind][0]}</span>}
